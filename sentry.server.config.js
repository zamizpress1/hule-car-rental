// Server-side Sentry configuration stub
// Note: This application is a Vite + React Single Page Application (SPA).
export const initServerSentry = () => {
  console.log('Server-side Sentry config (SPA environment)');
};
